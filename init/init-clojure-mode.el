;(autopair-mode -1)
;(require 'dash) ;;for smartparens
;(paredit-mode -1)
;(smartparens-strict-mode)
;(define-key clojure-test-mode-map (kbd "M-n") 'forward-char)

;; look into
;; http://clojurewebdev.com/workshop/slides/
;; (setq locate-command "mdfind")
;; (setq locate-make-command-line ("mdfind" ...))
